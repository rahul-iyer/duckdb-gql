# name: test/features/sqllogic/clauses/return-orderby/ReturnOrderBy6.test
# description: SQLLogicTest port specification for return-orderby/ReturnOrderBy6.feature
# group: [gql]
#
# GENERATED by scripts/generate_gql_clause_sqllogic.py; do not edit.
# Source: test/features/clauses/return-orderby/ReturnOrderBy6.feature
# Derived from the Apache-2.0 openCypher TCK corpus pinned in
# test/features/clauses/SOURCE.json; retain LICENSE and NOTICE.
#
# This file is the complete SQLLogic port specification, not an
# executable pass claim. Scenarios move into test/sql only after
# fixture adaptation and semantic assertions have been reviewed.

# Source: tck/features/clauses/return-orderby/ReturnOrderBy6.feature:33 [1] Handle constants and parameters inside an order by item which contains an aggregation expression
# Port status: pending
# Translation review flags: parameters
#
# GQL-adapted source specification:
#   Scenario: [1] Handle constants and parameters inside an order by item which contains an aggregation expression
#     Given an empty graph
#     And parameters are:
#       | age | 38 |
#     When executing query:
#       """
#       MATCH (person)
#       RETURN avg(person.age) AS avgAge
#       ORDER BY $age + avg(person.age) - 1000
#       """
#     Then the result should be, in any order:
#       | avgAge |
#       | null   |
#     And no side effects

# Source: tck/features/clauses/return-orderby/ReturnOrderBy6.feature:48 [2] Handle returned aliases inside an order by item which contains an aggregation expression
# Port status: pending
# Translation review flags: semantic_review
#
# GQL-adapted source specification:
#   Scenario: [2] Handle returned aliases inside an order by item which contains an aggregation expression
#     Given an empty graph
#     When executing query:
#       """
#       MATCH (me: Person)--(you: Person)
#       RETURN me.age AS age, count(you.age) AS cnt
#       ORDER BY age, age + count(you.age)
#       """
#     Then the result should be, in any order:
#       | age | cnt |
#     And no side effects

# Source: tck/features/clauses/return-orderby/ReturnOrderBy6.feature:60 [3] Handle returned property accesses inside an order by item which contains an aggregation expression
# Port status: pending
# Translation review flags: semantic_review
#
# GQL-adapted source specification:
#   Scenario: [3] Handle returned property accesses inside an order by item which contains an aggregation expression
#     Given an empty graph
#     When executing query:
#       """
#       MATCH (me: Person)--(you: Person)
#       RETURN me.age AS age, count(you.age) AS cnt
#       ORDER BY me.age + count(you.age)
#       """
#     Then the result should be, in any order:
#       | age | cnt |
#     And no side effects

# Source: tck/features/clauses/return-orderby/ReturnOrderBy6.feature:72 [4] Fail if not returned variables are used inside an order by item which contains an aggregation expression
# Port status: pending
# Translation review flags: semantic_review
#
# GQL-adapted source specification:
#   Scenario: [4] Fail if not returned variables are used inside an order by item which contains an aggregation expression
#     Given an empty graph
#     When executing query:
#       """
#       MATCH (me: Person)--(you: Person)
#       RETURN count(you.age) AS agg
#       ORDER BY me.age + count(you.age)
#       """
#     Then a SyntaxError should be raised at compile time: UndefinedVariable

# Source: tck/features/clauses/return-orderby/ReturnOrderBy6.feature:82 [5] Fail if more complex expressions, even if returned, are used inside an order by item which contains an aggregation expression
# Port status: pending
# Translation review flags: semantic_review
#
# GQL-adapted source specification:
#   Scenario: [5] Fail if more complex expressions, even if returned, are used inside an order by item which contains an aggregation expression
#     Given an empty graph
#     When executing query:
#       """
#       MATCH (me: Person)--(you: Person)
#       RETURN me.age + you.age, count(*) AS cnt
#       ORDER BY me.age + you.age + count(*)
#       """
#     Then a SyntaxError should be raised at compile time: AmbiguousAggregationExpression
